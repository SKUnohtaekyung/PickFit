{
  "name": "pickfit/app",
  "description": "PickFit PHP backend foundation for serving the SPA and JSON APIs.",
  "type": "project",
  "require": {
    "php": ">=8.2"
  },
  "autoload": {
    "psr-4": {
      "PickFit\\": "src/"
    }
  },
  "scripts": {
    "test": [
      "@php -l public/index.php"
    ]
  }
}
